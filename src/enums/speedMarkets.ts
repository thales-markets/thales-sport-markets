export enum WidgetMenuItems {
    TRADING = 'Trading',
    POSITIONS = 'Positions',
    SETTINGS = 'Settings',
}

export enum PositionsFilter {
    PENDING = 'pending',
    CLAIMABLE = 'claimable',
    HISTORY = 'history',
}

export enum SpeedPositions {
    UP = 'UP',
    DOWN = 'DOWN',
}
