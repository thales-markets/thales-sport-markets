export enum WidgetMenuItems {
    TRADING = 'Trading',
    POSITIONS = 'Positions',
    SETTINGS = 'Settings',
}

export enum PositionsFilter {
    PENDING = 'pending',
    CLAIMABLE = 'claimable',
    ALL = 'all',
}

export enum SpeedPositions {
    UP = 'UP',
    DOWN = 'DOWN',
}
