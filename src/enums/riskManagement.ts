export enum RiskManagementConfig {
    TEAMS = 'teams',
    BOOKMAKERS = 'bookmakers',
    SPREADS = 'spreads',
    LEAGUES = 'leagues',
}

export enum RiskManagementRole {
    ROOT_SETTING,
    RISK_MANAGING,
    MARKET_RESOLVING,
    TICKET_PAUSER,
}
