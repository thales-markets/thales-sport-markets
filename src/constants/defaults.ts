import { Network } from 'enums/network';

// ui defaults
export const DEFAULT_SEARCH_DEBOUNCE_MS = 300;
export const DEFAULT_TOKEN_DECIMALS = 18;

// network defaults
export const DEFAULT_NETWORK_ID = Network.OptimismMainnet;
